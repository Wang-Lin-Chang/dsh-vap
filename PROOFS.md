# VAP 共识层数学证明（PROOFS v2）

> 本文档给出 VAP 锁步 QC 链共识（Phase 5/6）的组合层安全性证明。
> 方法：手工归纳证明，零第三方依赖。每条假设显式编号；每条定理标注实现对照点。
> 纪律：禁止"显然/易知/类似可证"。

---

## 1. 记号、定义与公理

### 1.1 基本结构（公理 A0）

- 成员集合 R，|R| = n = 3f+1；拜占庭集合 F，|F| ≤ f（f 为容错预算）；
- 视图 v ∈ ℕ 全序；锁步轮次 leader = v mod n；
- 区块沿父指针成树，视图沿祖先链**严格递增**（允许跳轮/空轮；不要求连续）；
- 扩展关系 ⪰：B′ ⪰ B 当且仅当 B 是 B′ 的祖先（自反）；冲突 = 互不扩展。

### 1.2 证书（定义 D1）

- QC(B) = 对区块 B 的 ≥ 2f+1 个**不同签名者**的合法签名集合（签名者 ∈ R）；
- 签名绑定 (B, view(B))；Ed25519 不可伪造（公理 A1，密码学假设，本证明不证）；
- 诚实节点对同一视图至多签名一个区块；对同视图两个不同区块签名 = equivocation = 拜占庭行为，可检测并自动除名（公理 A2；**equivocation 按视图键控定义**）。

### 1.3 投票与锁（协议规则 P）

- **P1（安全规则）**：诚实节点只投"扩展其本地 lock 且扩展本地最高已认证区块"的提案；
- **P2（lock-on-vote，投票即锁）**：诚实节点投票给 B 的瞬间，将 lock 推进到 B 的父块；此后只投扩展 lock 的区块；
- **P3（3-chain 提交）**：区块 B 被提交 ⟺ 存在**直接父子链** B ← B₁ ← B₂（B₁ 为 B 的直接子块、B₂ 为 B₁ 的直接子块）且 QC(B)、QC(B₁)、QC(B₂) 均存在；
- **P4（视图切换）**：超时后 v+1，新提案携带发起者本地最高 QC。

> 实现对照点：P1/P2 对应 `phase5/vap-to.mjs` 的 `safetyRule()`（含 lock 扩展检查，L433-440）/`vote()`（lock 推进，L459-472）；P3 对应 `commitCheck()` 与 `findCertifiedChild()`（直接父子校验）；P4 对应 `onTimeout()`。

### 1.4 视图单调（公理 A3）

- **A3（视图单调投票）**：诚实节点仅在非递减的视图序列中投票；一旦在视图 v 投票，永不在 < v 的视图投票。

> 实现对照点：`safetyRule()` 的 `v < node.view` 拒绝分支。

### 1.5 成员变更（公理 A4-A5，仅 Phase 6 用）

- **A4（延迟生效）**：变更交易在高度 d 提交，R_new 在高度 d+2 生效；窗口 [d, d+2) 内签名按 R_old 判定；
- **A5（重叠约束）**：|R_old ∩ R_new| ≥ f_old + f_new + 1（引理 M1 的充要条件；当前实现单笔变更自动满足，多变更同块的显式校验见 §5 开放问题）。

> 实现对照点：`phase6/vap-to-membership.mjs` 的 `activateHeight = height + 2`。

---

## 2. 静态配置下的安全性

### 引理 L1（quorum 交集，纯计数）

**命题**：设 Q₁、Q₂ 为任意两个合法 quorum（各 ≥ 2f+1 个不同签名者），则 |Q₁ ∩ Q₂| ≥ f+1。

**证明**：|Q₁ ∩ Q₂| = |Q₁| + |Q₂| − |Q₁ ∪ Q₂| ≥ (2f+1) + (2f+1) − (3f+1) = f+1。∎

（紧性：取 Q₁ = {1..2f+1}、Q₂ = {f+1..3f+1}，交集恰为 f+1——下界不可再松。）

### 引理 L2（诚实交集）

**命题**：任意两个合法 quorum 的交集至少含 1 个诚实节点。

**证明**：反设 Q₁ ∩ Q₂ ⊆ F，则 |Q₁ ∩ Q₂| ≤ |F| ≤ f < f+1，与 L1 矛盾。∎

### 引理 L3（同视图唯一 QC）

**命题**：任意视图 v，至多一个区块获得 QC。

**证明**：反设 B ≠ B′ 同在视图 v 且各有 QC。由 L2，存在诚实节点 p 同时签名 B 与 B′；p 在同一视图签名两个不同区块 = equivocation，违反 A2（诚实节点不 equivocate）。∎

### 引理 L4（lock 链内单调）

**命题**：对任意诚实节点 i，其 lock 序列始终在同一链上（lock 永不离开当前链），lock 视图**非降**。

**证明**（对 i 的事件序列归纳）：
- 基态：lock = 创世块；
- 投票事件（P2）：新 lock = 所投区块的父块；由 P1，所投区块扩展旧 lock（注意：P1 要求扩展 lock 且视图严格高于 lock 视图——实现中 safetyRule 的祖先回溯检查排除"投 lock 自身"，因此父块必为 lock 或 lock 的严格后代），故新 lock ⪰ 旧 lock，链内推进、视图非降；
- 观察事件（收 QC）不改变 lock（P2 只在投票时推进）。
归纳得：lock 序列链内单调（视图非降，lock 值去重后严格递增）。∎

### 引理 L5（锁链冻结，核心桥接）

**命题**：设区块 B₁ 在视图 v₁ 有 QC，其**直接子块** B₂ 在视图 v₂ > v₁ 有 QC。则任何在视图 v* > v₂ 获得 QC 的区块 B* 必扩展 B₁。

**证明**：设 H 为 QC(B₂) 中的诚实投票者集合。由 |QC(B₂)| ≥ 2f+1 且 |F| ≤ f，|H| ≥ (2f+1) − f = f+1。
对任意 h ∈ H：由 A3（视图单调），h 投票 B₂（视图 v₂）先于其任何 v* > v₂ 的投票；h 投 B₂ 时由 P2 将 lock 推进到 B₂ 的父块 B₁；由 L4，h 的 lock 此后永不离开 B₁ 链。
反设 B* 在 v* > v₂ 有 QC 且不扩展 B₁：则 H 中无人投 B*（P1 要求扩展 lock，而 lock 在 B₁ 链上）；B* 最多获得 (n−f) − (f+1) = f 个诚实票 + f 个拜占庭票 = 2f < 2f+1 票，凑不出 QC，矛盾。故 B* 必扩展 B₁。∎

### 定理 T（3-chain 安全性）

**命题**：若某诚实节点提交区块 B，则不存在任何诚实节点提交与 B 冲突的区块 B′。

**证明**（反证 + 情形分析）：
设 i 提交 B（经直接父子链 B ← B₁ ← B₂，QC 三连，视图 v₀ < v₁ < v₂），j 提交 B′（经直接父子链 B′ ← B₁′ ← B₂′，视图 v₀′ < v₁′ < v₂′），且 B 与 B′ 冲突。
两提交对称，WLOG 设 v₂ ≤ v₂′（若 v₂ = v₂′ 由 L3 得 B₂ = B₂′，则 B 与 B′ 同为 B₂ 祖先，可比较，矛盾；故 v₂ < v₂′）。
对 B₁（v₁）与其直接子块 B₂（v₂）应用 L5（前提满足：B₁ 有 QC、B₂ 为其直接子块且有 QC、v₂ > v₁）：任何 v* > v₂ 的 QC 区块必扩展 B₁。取 B* = B₂′（视图 v₂′ > v₂，有 QC）：得 B₂′ ⪰ B₁ ⪰ B。
又 B₂′ ⪰ B′（B′ 是 B₂′ 的祖先）。故 B 与 B′ 同为 B₂′ 的祖先；单链祖先序是全序，B 与 B′ 可比较，与"冲突"矛盾。∎

### 推论 C1（无回滚）

**命题**：所有诚实节点提交的区块集合构成单一增长链；不存在已提交区块被回滚。

**证明**：由 T，任意两提交区块可比较（同链）；提交序列按视图单调（P3 的父子链定义的祖先序），故为唯一增长前缀。∎

---

## 3. 动态成员下的安全性（Phase 6）

### 引理 M1（quorum 重叠充要条件）

**命题**：对连续 roster R_old、R_new（各自 3f+1、quorum 2f+1），"任意旧 quorum 与任意新 quorum 交集非空"成立 ⟺ |R_old ∩ R_new| ≥ f_old + f_new + 1。

**证明**：
|Q_old ∩ Q_new| ≥ |Q_old| + |Q_new| − |R_old ∪ R_new|
≥ (2f_old+1) + (2f_new+1) − [(3f_old+1) + (3f_new+1) − |R_old ∩ R_new|]
= |R_old ∩ R_new| − (f_old + f_new)。
充分性：|R_old ∩ R_new| ≥ f_old + f_new + 1 ⇒ 交集 ≥ 1。
必要性：当 |R_old ∩ R_new| ≤ f_old + f_new 时，可铺满并集构造两不相交 quorum（取 a = max(0, c−f_old)、b = max(0, c−f_new)，a+b ≤ c，分别嵌入互斥段）。∎

### 引理 M2（诚实交集负结果）

**命题**（等规模变更 f_old = f_new = f）：任何非平凡成员变更（R_old ≠ R_new）下，"任意新旧 quorum 交集含诚实节点"**不成立**。

**证明**：要求任意 quorum 对含诚实节点，需 min |Q_old ∩ Q_new| ≥ f+1；由 M1 的一般化紧性，min |Q_old ∩ Q_new| = max(0, |R_old ∩ R_new| − 2f)，故需 |R_old ∩ R_new| ≥ 3f+1 = n，即两 roster 完全相同。任何真变更下该引理失效。∎

> **含义**：跨配置安全**不能**靠 quorum 交集证明（M2 负结果）。

### 定理 M3（延迟生效下的前缀延续——依赖 A5 重叠约束）

**命题**：在公理 A4（d+2 生效）、A5（重叠 ≥ f_old+f_new+1）、且**单笔变更**下，动态成员变更不破坏已提交前缀的安全性。

**证明**：
1. 变更交易 C 在高度 d 由 R_old 的 3-chain 提交定稿（A4）。由 C1，R_old 的已提交前缀为单链，C 在该链上；
2. 窗口 [d, d+2)：签名按 R_old 判定，安全性由定理 T 保障；
3. 高度 d+2 起 R_new 生效。R_new = R_old ∪ 新增成员（或减除名成员）。单笔 join：R_new ⊃ R_old，R_new 中的诚实旧成员 lock 已在 C 链上（L4），新成员首投受 P1 约束（lock 从创世起、只能投扩展链上块的提案——其首个最高 QC 来自旧链），诚实新成员亦不投冲突链；单笔 expel：R_new ⊂ R_old，直接由 T 的内部论证保障；
4. 由 A5 重叠约束 + T（R_new 内部）+ 步骤 3 的锁继承，任何新配置下的提交必须扩展 C 链；
5. 综上，新旧前缀连续、不冲突、不回滚。∎

> **边界声明**：M3 覆盖单笔变更（join 或 expel）；**多笔变更同块生效**的完整证明是开放问题（见 §5 开放问题第 1 条）。

---

## 4. 诚实边界

1. **活性不在此列**：本证明只证安全性（无冲突、无回滚）；活性（最终推进）依赖部分同步假设（GST 后诚实 leader 最终出现），该假设是活性论证的前提，不是本证明的对象；
2. **静态 vs 动态假设分离**：静态安全性（定理 T）不依赖同步/投递假设；动态安全性（M3）依赖 A4 的延迟生效与 A5 的重叠约束（均为安全假设，非活性假设）；
3. **公理外边界**：A1（Ed25519 不可伪造）与 OS 文件原子性是密码学/系统假设，不在本证明内；
4. **f+1 共谋**：≥ f+1 拜占庭节点可破坏安全——数学边界，本证明显式采用 |F| ≤ f；
5. **证明边界**：本证明是手工归纳证明（纯数学文本），尚未经证明助手（Coq/Lean）机器检查——这是下一步工作，不冒认；
6. **实现对照**：每条 P 规则标注了 `phase5/vap-to.mjs` 与 `phase6/vap-to-membership.mjs` 的对应函数（lock-on-vote 已落地，256 测试全绿）；若实现与 P1/P2 不符，本证明不适用于该实现；
7. **重启语义**：lock 不持久化，节点重启后从已提交前缀重建（lock 置 null 等价创世）——L4 的归纳在重启边界处重新起基，安全性不受影响（前缀由 C1 保障）。

## 4.1 TLC 机器检查实录（2026-08-25，tlaplus/TLC-RESULTS.md）

TLA+ 规格经 TLC 2.19 真实运行（老服务器 2 核/3.6GiB）：

- **安全不变式 7 条全部 PASS**：TypeOK / HonestVotePerView（A2）/ QcUniquePerView（L3）/ LockDominatesVotes（L4）/ CommittedImpliesCertified / NoConflictCommit（定理 T）/ NoRollback（C1）——Block=3/MaxView=2 完整穷举 80,820 状态「No error」；提交类三条在 Block=4/MaxView=3 非空洞规模 95.9 万状态未违约（部分检查）。
- **ViewProgress（活性，视图推进）PASS**：完整穷举 + WF(ViewChange) 直接可得。
- **EventuallyCommit（活性，最终提交）的最终状态（三层修复后，2026-08-25）**：基线反例（跳跃投票）经三层修复（baseline → Certified(parent) → view-change/new-view）全部消除，「稳定 leader + GST」下 TLC 机器验证 PASS（62,064 状态）；完全对抗模型 FAIL = FLP 下界（496,139 状态）。详见 §5 开放问题第 4 条。
- **Quorum 参数化边界（TLC 抓出的实现 bug，已修）**：`threshold = 2f+1` 仅在 n=3f+1 时等价于 ⌈2n/3⌉；n>3f+1（N=5/6）时诚实票下界过低，TLC 给出「同视图双 QC」（两个 QC 诚实票互不相交）诚实反例。实现已改为 `max(2f+1, ⌈2n/3⌉)`（phase5 L256、phase6 recomputeQuorum），回归测试钉死（N=5 拜占庭票分发下双 QC 均不成立）。
- **N 上限**：提交类性质（MaxView≥3）状态空间 ~10⁶，老服务器上实用 N 上限 = 4；N=5/6 的 Quorum 反例在 Block=3/MaxView=2 小规模即检出。

## 5. 开放问题

1. **多笔变更同块**：M3 覆盖单笔变更；同块内多次 join/expel 的 A5 重叠显式校验与完整证明待补；
2. **机器检查**：本证明待 Coq/Lean 形式化（手工证明 → 机器证明）；
3. **f_old ≠ f_new**：M2 仅证等规模变更的负结果；异规模变更的诚实交集下界公式待补；
4. **活性恢复（V2，三层全部落地，2026-08-25）**：TLC 实证活性缺口经三层修复：
   - **第一层 baseline 检查**：vote 前置「提案父块必须扩展本地最高 QC」（空基线仅允许 parent=GENESIS）——消除「跳跃投票」反例；TLC 安全回归 64,260 状态 PASS。
   - **第二层 Certified(parent)**：vote 前置「父块必须已认证」（提案携带父块 QC = justify 显式建模）——消除「投未认证后代」反例；TLC 安全回归 64,260 状态 PASS。
   - **第三层 view-change/new-view 协议**：view-change 消息携带本地最高 QC（签名绑定），新 leader 聚合取最高 QC 为锚——消除「跳视图死锁」；TLC：**稳定 leader + GST 下 EventuallyCommit PASS（62,064 状态）**，安全回归 55,188 状态 PASS。
   - **最终边界（如实）**：完全对抗模型 EventuallyCommit 仍 FAIL（496,139 状态）——拜占庭 leader 反复提父=Genesis 高视图块 + equivocate，属 FLP 下界（完全异步下无协议可消除），实现侧由 detectEquivocation 除名兜底。**「稳定 leader + 部分同步」下的活性已机器验证；完全对抗下的活性不可达是理论边界，非缺陷。**
