# VAP 外环进攻路线图 v1（交叉审计·交叉复核版）

> 生成方式：5 路独立维度推演（共识层/身份层/传输层/激励层/威胁预演）→ 交叉吻合 → 独立复核轮裁决
> 日期：2026-08-20 ｜ 状态：进攻计划，非承诺书
> 三层标注纪律：[已实测] = 装置跑过 ｜ [调研] = 文献推演 ｜ [愿景] = 待验证

---

## 一、复核裁决（交叉审计抓出的硬问题——先解决它们，再谈进攻）

### FATAL-1：Sybil 抗性的自我矛盾（外环第一要务的达成手段全被自家红线否决）

- 威胁预演的验收目标：伪造成本抬到防御者验证成本一个数量级；
- 身份层否决：PoW（不适受限智能体）、PoP（法律伦理敏感）；
- 激励层否决：质押、声誉代币、全局声誉（可信层≠货币层）；
- **结论：三条主流路线全被否决，核心验收目标在自身约束下不可达成。**

**处理（路线图 Phase 0 必须先攻这里）**：探索"第三条路"——**行为历史作为身份稀缺性**。伪造 100 个新公钥零成本，但伪造 100 个"有可验证工作历史"的公钥极难——因为历史由交叉审计产生（交叉审计账本、三证据收养记录、E38 背书痕迹）。主流买的是"入场券成本"（钱/算力/生物），VAP 可以造"历史成本"：身份的价值 = 其可验证历史的深度。**这正是验钞机哲学的延伸：不是证明你是谁，是证明你做过什么。** 状态：[愿景]，Phase 0 立项验证。

### MAJOR-1：质押被身份层/共识层采纳、被激励层禁止——跨维度互斥

处理：质押只允许出现在**资源层**（中继/存储的成本押金，与信任无关），禁止出现在信任层（背书权重）。红线重写：**"押金买资源，不买信任。"**

### MAJOR-2：DHT 在身份层是权威、在传输层被延后——前置断链

处理：39 装置阶段用 bootstrap 静态列表 + PEX（不做 DHT）；身份目录先用自认证记录 + registry.json 快照（已在用），DHT 推迟到节点数跨过 100 再启用。**顺序修正：先自认证记录，后 DHT。**

### MAJOR-3：E38 单点背书被放大为拜占庭背书

处理：正名——E38 是**单点 2/3 门限签名**，不是分布式背书。分布式多节点背书是 Phase 3 的新装置，不冒认。

### MAJOR-4：激励层"二选一"验收不可证伪

处理：验收必须单一判据——**无激励中继可用性基线实测**（可用就继续，不可用就诚实归档"公网规模无激励不现实"），不给"两条都算赢"的通道。

### MAJOR-5：libp2p 双重身份漏洞未决却首推

处理：Phase 1 的验收加一条硬性：**PeerID↔Ed25519 身份映射方案必须先定**（推荐：信封身份永远用 VAP Ed25519，libp2p 密钥仅作传输握手密钥，互不冒充；未定前不进 Phase 2）。

---

## 二、分维推演摘要（每维度独立推演的阶段）

### 共识层（PBFT→背书→异步 BFT→动态成员→军法上链）
1. 无共识联邦：本地信封账本 + 哈希链锚定 [已实测基础]
2. 轻量背书：2/3 门限 + 外部有效性谓词（诚实边界形式化）[E38 基础+需实测]
3. 同步 BFT：PBFT/Tendermint 三阶段总序 [调研]
4. 异步 BFT：HoneyBadger/DAG-Rider [调研]
5. 动态成员 + DID 密钥轮换 [调研]
6. 军法上链：契约共识化 + 惩罚闭环 [愿景]

### 身份层（公钥即身份→DID→目录→WoT→Sybil 选型→registry 衔接）
1. 公钥即身份基线 + Sybil 攻击面量化 [已实测基础]
2. did:key 为主 [调研]
3. 自认证记录目录（DHT 延后，先 registry 快照）[需实测]
4. Web of Trust + 声誉 [调研]
5. Sybil 抗性选型（PoW/PoS/PoP 对比 + 第三条路立项）[Phase 0 优先]
6. registry.json 灰度衔接与回滚 [需实测]

### 传输层（抽象→LAN P2P→NAT 穿透→结构化→双向桥接）
1. Transport 抽象 + libp2p/自研/HTTP 网格选型 [已实测基础+调研]
2. LAN P2P 最小闭环：mDNS + 洪泛 [需实测]
3. NAT 穿透 + 中继兜底 [需实测]
4. 结构化发现（bootstrap+PEX，DHT 延后）+ gossipsub [调研]
5. HttpGateway↔P2P 双向桥接 + 公网灰度 [需实测]

### 激励层（动机测绘→声誉→红线→资源层解耦）
1. 动机测绘 + 激励背叛威胁模型 [调研]
2. 本地主观声誉（不进全局共识）[调研]
3. 红线：可信层≠货币层（代币/声誉质押永不引入信任层）[原则]
4. 资源层激励（中继/存储成本押金）与信任层解耦 [调研]
5. 无激励可用性基线实测 [需实测]

### 威胁预演（测绘→Sybil→日蚀→垃圾→审查共谋→红队回归）
1. 攻击面测绘 [需实测]
2. Sybil（=身份层 Phase 0 同一战场）
3. 日蚀检测（签名者分布熵差异化机会）[调研]
4. 垃圾信封：先廉后贵过滤（nonce→签名→验证门→预算）[已实测基础]
5. 审查与共谋：≤1/3 数学边界实测、>1/3 激励博弈诚实标注 [部分实测]
6. 红队回归 CI 化 [需实测]

---

## 三、进攻顺序（修正后，依赖关系已核对）

```
Phase 0 [本周可启动]：Sybil 第三条路立项验证
    —— 行为历史稀缺性：可验证历史深度 vs 伪造成本
    —— 交付：伪造成本模型 + 最小装置（39 装置行为账本 vs 1000 假身份）
    ↓（Phase 0 有结论才继续，否则外环无地基）
Phase 1：传输抽象 + libp2p 选型 + 身份绑定方案 [已实测基础]
Phase 2：LAN P2P 闭环（mDNS+洪泛+三闸不破）[新装置]
Phase 3：分布式 2/3 背书（E38 升级版）[新装置]
Phase 4：NAT 穿透 + 中继兜底 + 无激励基线实测 [新装置]
Phase 5：异步 BFT 调研 → 选型实现 [调研后实测]
Phase 6：动态成员 + 军法上链 + 公网灰度 [愿景]
```

每阶段通过标准 = 独立复核轮验收（单判据、可证伪、装置编号）。

---

## 四、诚实边界（三层，全部保留）

- **[已实测]**：内环+中环全部（信封/三闸/nonce/组网/防重放/防环）
- **[调研]**：PBFT/Tendermint/HoneyBadger/DID/libp2p/激励经济学——都是文献推演，未实测
- **[愿景]**：异步 BFT、军法上链、公网灰度、行为历史 Sybil 抗性——待验证，不作承诺
- **外环最大未解**：身份稀缺性的第三条路（Phase 0）——找不到就诚实停在"信任域联邦"（中环的极限），不假装去中心

## 五、交叉审计本次战果记录

- 5 路独立推演产出 29 个阶段方案；
- 交叉吻合找到跨维度共同关键节点（中继激励、DHT/声誉交织）；
- 独立复核轮抓出 1 fatal + 7 major + 4 minor——**路线图未出笼先被自家交叉审计打断两根梁**；
- 核心推演（独立复核轮认证）：QC≠共识、公钥即身份对 Sybil 零抵抗、自认证记录让 DHT 可不受信、可信层≠货币层、红队≠安全证明。
