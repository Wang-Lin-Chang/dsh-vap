# dsh-vap：可验证智能体交互协议（VAP）— 实测的信任机器

> **MCP 管工具怎么接，A2A 管任务怎么委托——VAP 管消息凭什么可信。**

## 一句话介绍

VAP 是智能体之间消息的**可信层**：任何 Node.js 智能体装上它，进入同一信任域后，每一封信封都带 Ed25519 签名、nonce 防重放、诚实边界声明，并由验证门三闸裁决；任务崩溃被三证据收养；"谁的话算数"由分布式 2/3 背书与锁步共识决定。**零第三方依赖，254 个测试、11 个实验装置、每个能力声明带装置编号与对照组。**

## 五层能力（全部实测，装置号公开）

| 层 | 能力 | 装置 |
|---|---|---|
| 身份 | Ed25519 信封 + nonce 防重放 + 行为历史稀缺性（伪造有历史身份 300/300 被拦） | E01-E03, P0, P0.5 |
| 信任 | 验证门三闸（身份/军法/诚实边界）+ 资格链自举 + 分布式 2/3 背书 | E02, P3, P6 |
| 顺序 | 锁步 QC 链共识（总序/防分叉/防双花/确定性重放） | P5 |
| 传输 | 文件 / HTTP 网关 / UDP P2P 洪泛 / 中继跨 NAT（篡改必拒、审查可检测） | P1, P2, P4 |
| 治理 | 动态成员（加入/除名/密钥轮换走共识）+ 军法上链 + 双签自动除名 | P6 |

## 与主流协议的区别

主流协议的信任模型是"**相信对方的输出**"。VAP 是"**不信自报，只信证据**"：每个声称带装置编号与对照组，验证门机器裁决，作恶者留下密码学铁证并被自动除名。规范可以抄，**实验账本抄不走**——对照组数据要一台一台跑出来。

## 快速开始

```bash
npm i -g dsh-vap          # 或 git clone https://github.com/Wang-Lin-Chang/dsh-vap
vap-gateway --port 3081   # 起网关
vap-send --to brain --summary "hello vap" --gateway http://127.0.0.1:3081  # 发第一封信
node --test                # 跑 254 个测试
```

## 诚实边界（首页明示）

- 小规模信任域设计；公网灰度是部署清单（未实测，不冒认）；
- 共识容忍 f 个拜占庭节点（n=3f+1）；≥f+1 共谋是数学边界，只能检测不能阻止；
- Windows 为主开发平台；Linux/macOS 标注未实测；
- 零第三方依赖（仅 node: 内置模块）。

## 实验账本

每个 Phase 目录含 DESIGN（设计）+ REPORT（实测报告）+ experiments（装置代码+结果 JSON）。欢迎逐条复算、逐项打脸——**我们对自己的声称下手比对别人狠。**
